# servicenow-eval task specs (v3 — 20 hardened tasks, 68 pts)

Parse the JSON in the fenced code block below. (v3 = eval v6.1 hardening: derived values, near-miss distractors, negative asserts, execution-based grading. Grader-private seed snapshots live in x_eval.tN.seed — never surfaced in setup output.)

```json
{
  "version": 3,
  "total_points": 68,
  "tasks": [
    {
      "id": "T1",
      "name": "Filtered incident count with distractors",
      "category": "read",
      "points": 2,
      "prompt": "Count the incidents that are active, have priority 1 or 2, and whose short_description begins with the exact marker '[EVAL-H1] ' (tag + one space). Write the count as a string to the system property 'x_eval.h1.result'. Do not create, modify, or delete any incident.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nfunction mkInc(d,im,ur,close,cat){var n=new GlideRecord('incident');n.initialize();n.short_description=d;n.impact=im;n.urgency=ur;if(cat)n.category=cat;if(close){n.state=7;n.active=false;n.close_code='Closed/Resolved by Caller';n.close_notes='eval seed close';}var id=n.insert()+'';var r=new GlideRecord('incident');r.get(id);return{id:id,a:r.active+'',p:r.priority+'',m:r.sys_mod_count+'',num:r.number+''};}\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H1');wq.query();while(wq.next())wq.deleteRecord();\ndelPfx('x_eval.t1.');delPfx('x_eval.h1.');\nvar recs=[];var g=gs.generateGUID();\nvar nA=2+Math.floor(Math.random()*5);\nfor(var i=0;i<nA;i++)recs.push(mkInc('[EVAL-H1] Alert '+i+' '+g.substring(0,6),1,(Math.random()<0.5?1:2),false,'software'));\nvar nB=1+Math.floor(Math.random()*3);\nfor(var j=0;j<nB;j++)recs.push(mkInc('[EVAL-H1] Closed '+j+' '+g.substring(6,12),1,1,true,'software'));\nvar nC=1+Math.floor(Math.random()*3);\nfor(var k=0;k<nC;k++){var iu=(Math.random()<0.5?2:3);recs.push(mkInc('[EVAL-H1] Minor '+k+' '+g.substring(12,18),iu,iu,false,'software'));}\nvar nD=1+Math.floor(Math.random()*2);\nfor(var m2=0;m2<nD;m2++)recs.push(mkInc('[EVAL-H1X] Alert '+m2+' '+g.substring(18,24),1,1,false,'software'));\nsetP('x_eval.t1.seed',JSON.stringify({recs:recs,pop:recs.length}));\ngs.print(JSON.stringify({seeded:true,marker:'[EVAL-H1] '}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t1.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar c=0;var tot=0;var q=new GlideRecord('incident');q.addQuery('short_description','STARTSWITH','[EVAL-H1');q.query();\nwhile(q.next()){tot++;var d=q.short_description+'';var pr=q.priority+'';if(d.indexOf('[EVAL-H1] ')===0&&(q.active+'')==='true'&&(pr==='1'||pr==='2'))c++;}\nchk('population',tot===seed.pop);\nfor(var i=0;i<seed.recs.length;i++){var rc=seed.recs[i];var r=new GlideRecord('incident');if(!r.get(rc.id)){bad.push('missing'+i);continue;}if((r.active+'')!==rc.a||(r.priority+'')!==rc.p||(r.sys_mod_count+'')!==rc.m)bad.push('touched'+i);}\nvar got=getP('x_eval.h1.result');chk('result',got===String(c));\ngs.print(JSON.stringify({pass:bad.length===0,expected:'result='+c+' pop='+seed.pop,actual:bad.length?('FAIL:'+bad.join(',')):('result='+got)}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nfunction getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\ntry{var s1=JSON.parse(getP('x_eval.t1.seed'));if(s1&&s1.recs){for(var i=0;i<s1.recs.length;i++){var r=new GlideRecord('incident');if(r.get(s1.recs[i].id))r.deleteRecord();}}}catch(e){}\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H1');wq.query();while(wq.next())wq.deleteRecord();\ndelPfx('x_eval.t1.');delPfx('x_eval.h1.');"
    },
    {
      "id": "T2",
      "name": "Create incident from derived values",
      "category": "write",
      "points": 3,
      "prompt": "Create exactly ONE incident: its short_description must be '[EVAL-H2] ' followed immediately by the value of the system property 'x_eval.h2.token'; its caller_id must be the user whose email is exactly the value of the system property 'x_eval.h2.caller_email'; it must END UP with priority 1 (work with the platform's impact/urgency priority rules, not against them); and its category must be 'software'. Store the new incident's sys_id in the system property 'x_eval.h2.result'.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H2');wq.query();while(wq.next())wq.deleteRecord();\nvar wu=new GlideRecord('sys_user');wu.addQuery('user_name','STARTSWITH','eval.h2.');wu.query();while(wu.next())wu.deleteRecord();\ndelPfx('x_eval.t2.');delPfx('x_eval.h2.');\nvar tok=gs.generateGUID().substring(0,8);\nfunction mkU(un,fn,ln,em){var u=new GlideRecord('sys_user');u.initialize();u.user_name=un;u.first_name=fn;u.last_name=ln;u.email=em;u.active=true;return u.insert()+'';}\nvar idA=mkU('eval.h2.a','Eval','H2 Alpha','eval.h2.a@evalh2.example');\nvar idB=mkU('eval.h2.b','Eval','H2 Bravo','eval-h2.a@evalh2.example');\nvar pick=Math.random()<0.5;var target=pick?idA:idB;var other=pick?idB:idA;var email=pick?'eval.h2.a@evalh2.example':'eval-h2.a@evalh2.example';\nsetP('x_eval.h2.token',tok);setP('x_eval.h2.caller_email',email);\nsetP('x_eval.t2.seed',JSON.stringify({tok:tok,target:target,other:other}));\ngs.print(JSON.stringify({seeded:true,props:['x_eval.h2.token','x_eval.h2.caller_email']}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t2.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar desc='[EVAL-H2] '+seed.tok;var n=0;var rec=null;\nvar q=new GlideRecord('incident');q.addQuery('short_description',desc);q.query();while(q.next()){n++;if(n===1){rec=new GlideRecord('incident');rec.get(q.sys_id+'');}}\nchk('exactly_one',n===1);\nif(rec){\nchk('result_prop',getP('x_eval.h2.result')===(rec.sys_id+''));\nchk('caller',(rec.caller_id+'')===seed.target);\nchk('priority',(rec.priority+'')==='1');chk('impact',(rec.impact+'')==='1');chk('urgency',(rec.urgency+'')==='1');\nchk('category',(rec.category+'')==='software');\n}\nvar q2=new GlideRecord('incident');q2.addQuery('caller_id',seed.other);q2.query();var n2=0;while(q2.next())n2++;chk('nearmiss_user_untouched',n2===0);\ngs.print(JSON.stringify({pass:bad.length===0,expected:'1 incident, caller+P1+category correct',actual:bad.length?('FAIL:'+bad.join(',')):('ok n='+n)}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nfunction getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H2');wq.query();while(wq.next())wq.deleteRecord();\ntry{var s=JSON.parse(getP('x_eval.t2.seed'));if(s){var u1=new GlideRecord('sys_user');if(u1.get(s.target))u1.deleteRecord();var u2=new GlideRecord('sys_user');if(u2.get(s.other))u2.deleteRecord();}}catch(e){}\nvar wu=new GlideRecord('sys_user');wu.addQuery('user_name','STARTSWITH','eval.h2.');wu.query();while(wu.next())wu.deleteRecord();\ndelPfx('x_eval.t2.');delPfx('x_eval.h2.');"
    },
    {
      "id": "T3",
      "name": "Link problem to the qualifying incident",
      "category": "link",
      "points": 3,
      "prompt": "Exactly one incident whose short_description starts with '[EVAL-H3] ' (tag + one space) is both active and priority 1. Create ONE problem record whose short_description is exactly '[EVAL-H3] RCA ' followed by that incident's number, and set that incident's problem_id to the new problem. Do not modify any other record. Store the new problem's sys_id in the system property 'x_eval.h3.result'.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nfunction mkInc(d,im,ur,close,cat){var n=new GlideRecord('incident');n.initialize();n.short_description=d;n.impact=im;n.urgency=ur;if(cat)n.category=cat;if(close){n.state=7;n.active=false;n.close_code='Closed/Resolved by Caller';n.close_notes='eval seed close';}var id=n.insert()+'';var r=new GlideRecord('incident');r.get(id);return{id:id,a:r.active+'',p:r.priority+'',m:r.sys_mod_count+'',num:r.number+''};}\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H3');wq.query();while(wq.next())wq.deleteRecord();\nvar wp=new GlideRecord('problem');wp.addQuery('short_description','STARTSWITH','[EVAL-H3');wp.query();while(wp.next())wp.deleteRecord();\ndelPfx('x_eval.t3.');delPfx('x_eval.h3.');\nvar g=gs.generateGUID();\nvar target=mkInc('[EVAL-H3] Payment outage '+g.substring(0,6),1,1,false,'software');\nvar t1=mkInc('[EVAL-H3] Archived failure '+g.substring(6,12),1,1,true,'software');\nvar t2=mkInc('[EVAL-H3] Degraded service '+g.substring(12,18),1,2,false,'software');\nvar t3=mkInc('[EVAL-H3X] Payment outage '+g.substring(18,24),1,1,false,'software');\nsetP('x_eval.t3.seed',JSON.stringify({target:target,traps:[t1,t2,t3]}));\ngs.print(JSON.stringify({seeded:true,marker:'[EVAL-H3] '}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t3.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar n=0;var prb=null;var q=new GlideRecord('problem');q.addQuery('short_description','STARTSWITH','[EVAL-H3');q.query();while(q.next()){n++;if(n===1){prb=new GlideRecord('problem');prb.get(q.sys_id+'');}}\nchk('exactly_one_problem',n===1);\nif(prb){\nchk('problem_desc',(prb.short_description+'')===('[EVAL-H3] RCA '+seed.target.num));\nchk('result_prop',getP('x_eval.h3.result')===(prb.sys_id+''));\nvar t=new GlideRecord('incident');chk('link',t.get(seed.target.id)&&(t.problem_id+'')===(prb.sys_id+''));\n}\nfor(var i=0;i<seed.traps.length;i++){var tp=seed.traps[i];var r=new GlideRecord('incident');if(!r.get(tp.id)){bad.push('trapmissing'+i);continue;}if((r.problem_id+'')!==''||(r.sys_mod_count+'')!==tp.m)bad.push('trap'+i);}\ngs.print(JSON.stringify({pass:bad.length===0,expected:'1 problem RCA '+seed.target.num+' linked',actual:bad.length?('FAIL:'+bad.join(',')):('ok')}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wp=new GlideRecord('problem');wp.addQuery('short_description','STARTSWITH','[EVAL-H3');wp.query();while(wp.next())wp.deleteRecord();\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H3');wq.query();while(wq.next())wq.deleteRecord();\ndelPfx('x_eval.t3.');delPfx('x_eval.h3.');"
    },
    {
      "id": "T4",
      "name": "Fix subtly buggy script include",
      "category": "debug",
      "points": 5,
      "prompt": "The global script include 'EvalH4Util' has a method normalizeEmails(input) which is SUPPOSED to: split the input string on semicolons, trim each part, drop empty parts, lowercase everything, remove duplicates case-insensitively, and return the resulting array sorted alphabetically. Users report duplicate and lost entries. Fix 'EvalH4Util' so normalizeEmails behaves exactly as specified for any input. Do NOT rename the script include or the method, and do NOT modify any other script include.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wi=new GlideRecord('sys_script_include');wi.addQuery('name','STARTSWITH','EvalH4Util');wi.query();while(wi.next())wi.deleteRecord();\ndelPfx('x_eval.t4.');delPfx('x_eval.h4.');\nfunction mkSI(nm,src){var n=new GlideRecord('sys_script_include');n.initialize();n.name=nm;n.api_name='global.'+nm;n.sys_scope='global';n.client_callable=false;n.access='public';n.script=src;return n.insert()+'';}\nvar buggy=\"var EvalH4Util = Class.create();\\nEvalH4Util.prototype = {\\n    initialize: function() {},\\n    normalizeEmails: function(input) {\\n        var parts = String(input || '').split(';');\\n        var seen = {};\\n        var out = [];\\n        for (var i = 0; i < parts.length - 1; i++) {\\n            var e = parts[i].trim();\\n            if (seen[e]) continue;\\n            seen[e] = true;\\n            out.push(e);\\n        }\\n        return out.sort();\\n    },\\n    type: 'EvalH4Util'\\n};\";\nvar decoy=\"var EvalH4Utils = Class.create();\\nEvalH4Utils.prototype = {\\n    initialize: function() {},\\n    normalizeEmails: function(input) {\\n        var parts = String(input || '').split(';');\\n        var seen = {};\\n        var out = [];\\n        for (var i = 0; i < parts.length; i++) {\\n            var e = parts[i].trim().toLowerCase();\\n            if (!e || seen[e]) continue;\\n            seen[e] = true;\\n            out.push(e);\\n        }\\n        return out.sort();\\n    },\\n    type: 'EvalH4Utils'\\n};\";\nmkSI('EvalH4Util',buggy);var decoyId=mkSI('EvalH4Utils',decoy);\nsetP('x_eval.t4.seed',JSON.stringify({decoyId:decoyId,decoy:decoy}));\ngs.print(JSON.stringify({seeded:true,script_include:'EvalH4Util'}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t4.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar dg=new GlideRecord('sys_script_include');\nif(!dg.get('name','EvalH4Utils')){bad.push('decoy_missing');}else{chk('decoy_untouched',(dg.script+'')===seed.decoy);}\nvar mg=new GlideRecord('sys_script_include');chk('include_exists',mg.get('name','EvalH4Util'));\nvar cases=[['A@b.com;a@B.com','a@b.com'],[' a@B.com ;;b@c.com; ','a@b.com|b@c.com'],['',''],['x@y.com;X@Y.COM;x@y.com','x@y.com'],['\\tc@d.com\\t;a@b.com','a@b.com|c@d.com'],['z@z.com;m@m.com;','m@m.com|z@z.com'],['solo@one.com','solo@one.com']];\ntry{var u=new EvalH4Util();for(var i=0;i<cases.length;i++){var r=u.normalizeEmails(cases[i][0]);var got=(r!=null&&typeof r.join==='function')?r.join('|'):('notarray');if(got!==cases[i][1])bad.push('case'+(i+1));}}catch(e2){bad.push('exec:'+String(e2).substring(0,30));}\ngs.print(JSON.stringify({pass:bad.length===0,expected:'7 hidden cases + decoy untouched',actual:bad.length?('FAIL:'+bad.join(',')):('all cases pass')}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wi=new GlideRecord('sys_script_include');wi.addQuery('name','STARTSWITH','EvalH4Util');wi.query();while(wi.next())wi.deleteRecord();\ndelPfx('x_eval.t4.');delPfx('x_eval.h4.');"
    },
    {
      "id": "T5",
      "name": "Selective batch resolve",
      "category": "batch",
      "points": 4,
      "prompt": "Resolve ONLY the incidents whose short_description starts with '[EVAL-H5] ' (tag + one space) that are active, not already resolved (state < 6), and whose category is NOT 'network'. For each of those: set state=6 (Resolved), close_code='Solved (Permanently)', and close_notes='[EVAL-H5] <number> resolved' where <number> is that incident's own number field (e.g. INC0010042). Leave every other incident untouched. Write the count of incidents you resolved, as a string, to the system property 'x_eval.h5.result'.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nfunction mkInc(d,im,ur,close,cat){var n=new GlideRecord('incident');n.initialize();n.short_description=d;n.impact=im;n.urgency=ur;if(cat)n.category=cat;if(close){n.state=7;n.active=false;n.close_code='Closed/Resolved by Caller';n.close_notes='eval seed close';}var id=n.insert()+'';var r=new GlideRecord('incident');r.get(id);return{id:id,a:r.active+'',p:r.priority+'',m:r.sys_mod_count+'',num:r.number+''};}\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H5');wq.query();while(wq.next())wq.deleteRecord();\ndelPfx('x_eval.t5.');delPfx('x_eval.h5.');\nvar g=gs.generateGUID();var cats=['hardware','software','inquiry'];var el=[];\nvar nel=4+Math.floor(Math.random()*3);\nfor(var i=0;i<nel;i++){var e=mkInc('[EVAL-H5] Ticket '+i+' '+g.substring(0,6),2,2,false,cats[i%3]);el.push({id:e.id,num:e.num});}\nvar traps=[];\nvar tr=new GlideRecord('incident');tr.initialize();tr.short_description='[EVAL-H5] Ticket R '+g.substring(6,12);tr.impact=2;tr.urgency=2;tr.category='hardware';tr.state=6;tr.close_code='Solved (Work Around)';tr.close_notes='[EVAL-H5] pre-resolved';var trId=tr.insert()+'';var trr=new GlideRecord('incident');trr.get(trId);traps.push({id:trId,m:trr.sys_mod_count+''});\nvar tn=mkInc('[EVAL-H5] Ticket N '+g.substring(12,18),2,2,false,'network');traps.push({id:tn.id,m:tn.m});\nvar tc=mkInc('[EVAL-H5] Ticket C '+g.substring(18,24),2,2,true,'software');traps.push({id:tc.id,m:tc.m});\nsetP('x_eval.t5.seed',JSON.stringify({el:el,n:el.length,traps:traps}));\ngs.print(JSON.stringify({seeded:true,marker:'[EVAL-H5] '}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t5.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nfor(var i=0;i<seed.el.length;i++){var e=seed.el[i];var r=new GlideRecord('incident');if(!r.get(e.id)){bad.push('elmissing'+i);continue;}\nif((r.state+'')!=='6'||(r.close_code+'')!=='Solved (Permanently)'||(r.close_notes+'')!==('[EVAL-H5] '+e.num+' resolved'))bad.push('el'+i);}\nfor(var j=0;j<seed.traps.length;j++){var tp=seed.traps[j];var t=new GlideRecord('incident');if(!t.get(tp.id)){bad.push('trapmissing'+j);continue;}if((t.sys_mod_count+'')!==tp.m)bad.push('trap'+j);}\nchk('count_prop',getP('x_eval.h5.result')===String(seed.n));\ngs.print(JSON.stringify({pass:bad.length===0,expected:seed.n+' resolved, traps untouched',actual:bad.length?('FAIL:'+bad.join(',')):('ok n='+seed.n)}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H5');wq.query();while(wq.next())wq.deleteRecord();\ndelPfx('x_eval.t5.');delPfx('x_eval.h5.');"
    },
    {
      "id": "T6",
      "name": "Insert-only conditional business rule",
      "category": "business_rule",
      "points": 4,
      "prompt": "Create an ACTIVE Business Rule on the 'incident' table named '[EVAL-H6] Normalizer' that runs BEFORE INSERT (insert only — it must NOT run on update) and uppercases the short_description when — and only when — the short_description starts with '[EVAL-H6]'. It must not affect any other incident and must not change existing records. Behavior is probed: a matching insert must come out uppercased; a non-matching insert must be unchanged; an update of a matching record must NOT be re-uppercased.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wb=new GlideRecord('sys_script');wb.addQuery('name','STARTSWITH','[EVAL-H6]');wb.query();while(wb.next())wb.deleteRecord();\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H6');wq.query();while(wq.next())wq.deleteRecord();\ndelPfx('x_eval.t6.');delPfx('x_eval.h6.');\nvar n=new GlideRecord('incident');n.initialize();n.short_description='[EVAL-H6] legacy record';n.impact=2;n.urgency=2;var id=n.insert()+'';\nvar r=new GlideRecord('incident');r.get(id);\nsetP('x_eval.t6.seed',JSON.stringify({legacy:{id:id,d:r.short_description+'',m:r.sys_mod_count+''}}));\ngs.print(JSON.stringify({seeded:true,br_name:'[EVAL-H6] Normalizer'}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t6.seed'))||null;}catch(e){return null;}}\nvar pids=[];\nfunction delProbes(){for(var x=0;x<pids.length;x++){try{var dr=new GlideRecord('incident');if(dr.get(pids[x]))dr.deleteRecord();}catch(ed){}}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar nbr=0;var br=null;var bq=new GlideRecord('sys_script');bq.addQuery('name','[EVAL-H6] Normalizer');bq.addQuery('collection','incident');bq.query();while(bq.next()){nbr++;if(nbr===1){br=new GlideRecord('sys_script');br.get(bq.sys_id+'');}}\nchk('exactly_one_br',nbr===1);\nif(br){chk('when_before',(br.when+'')==='before');chk('on_insert',(br.action_insert+'')==='true');chk('not_on_update',(br.action_update+'')==='false');chk('br_active',(br.active+'')==='true');}\nfunction ins(d){var p=new GlideRecord('incident');p.initialize();p.short_description=d;p.impact=2;p.urgency=2;var id=p.insert()+'';pids.push(id);return id;}\nfunction dsc(id){var r=new GlideRecord('incident');return r.get(id)?(r.short_description+''):'__gone__';}\nvar p1=ins('[EVAL-H6] probe alpha mixed');chk('match_insert_uppercased',dsc(p1)==='[EVAL-H6] PROBE ALPHA MIXED');\nvar p2=ins('h6 plain probe sample');chk('nonmatch_insert_unchanged',dsc(p2)==='h6 plain probe sample');\nvar r1=new GlideRecord('incident');if(r1.get(p1)){r1.short_description='[EVAL-H6] still lower';r1.update();}\nchk('update_does_not_fire',dsc(p1)==='[EVAL-H6] still lower');\nvar lg=new GlideRecord('incident');if(!lg.get(seed.legacy.id)){bad.push('legacy_missing');}else{if((lg.short_description+'')!==seed.legacy.d||(lg.sys_mod_count+'')!==seed.legacy.m)bad.push('legacy_touched');}\ndelProbes();\ngs.print(JSON.stringify({pass:bad.length===0,expected:'BR insert-only conditional, probes pass',actual:bad.length?('FAIL:'+bad.join(',')):('ok')}));\n}\n}catch(e){try{delProbes();}catch(e9){}gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nfunction getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar wb=new GlideRecord('sys_script');wb.addQuery('name','STARTSWITH','[EVAL-H6]');wb.query();while(wb.next())wb.deleteRecord();\ntry{var s=JSON.parse(getP('x_eval.t6.seed'));if(s&&s.legacy){var lr=new GlideRecord('incident');if(lr.get(s.legacy.id))lr.deleteRecord();}}catch(e){}\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H6');wq.query();while(wq.next())wq.deleteRecord();\nvar wpp=new GlideRecord('incident');wpp.addQuery('short_description','h6 plain probe sample');wpp.query();while(wpp.next())wpp.deleteRecord();\ndelPfx('x_eval.t6.');delPfx('x_eval.h6.');"
    },
    {
      "id": "T7",
      "name": "Field ACL with exact role set",
      "category": "acl",
      "points": 4,
      "prompt": "Create the field-level ACL that restricts WRITE on incident.urgency: a sys_security_acl with type='record', operation='write', name='incident.urgency', active=true, admin_overrides=false, requiring BOTH roles 'itil' AND 'x_eval_h7_ops' (linked via sys_security_acl_role). Do not create, modify, or delete any other ACL. NOTE: creating ACLs requires elevated 'security_admin' privileges — elevate first if needed.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nfunction wipeAcl(nm,op){var a=new GlideRecord('sys_security_acl');a.addQuery('name',nm);if(op)a.addQuery('operation',op);a.query();while(a.next()){var rg=new GlideRecord('sys_security_acl_role');rg.addQuery('sys_security_acl',a.sys_id+'');rg.query();while(rg.next())rg.deleteRecord();a.deleteRecord();}}\nwipeAcl('incident.urgency','write');\nvar da=new GlideRecord('sys_security_acl');da.addQuery('description','STARTSWITH','[EVAL-H7]');da.query();while(da.next()){var drg=new GlideRecord('sys_security_acl_role');drg.addQuery('sys_security_acl',da.sys_id+'');drg.query();while(drg.next())drg.deleteRecord();da.deleteRecord();}\nvar orl=new GlideRecord('sys_user_role');orl.addQuery('name','STARTSWITH','x_eval_h7');orl.query();while(orl.next()){var hg=new GlideRecord('sys_user_has_role');hg.addQuery('role',orl.sys_id+'');hg.query();while(hg.next())hg.deleteRecord();orl.deleteRecord();}\ndelPfx('x_eval.t7.');delPfx('x_eval.h7.');\nvar rr=new GlideRecord('sys_user_role');rr.initialize();rr.name='x_eval_h7_ops';rr.description='Eval H7 ops role';var roleId=rr.insert()+'';\nvar dc=new GlideRecord('sys_security_acl');dc.initialize();dc.name='incident.impact';dc.operation='write';dc.type='record';dc.active=true;dc.description='[EVAL-H7] decoy - do not modify';var dcId=dc.insert()+'';\nvar dcr=new GlideRecord('sys_security_acl');dcr.get(dcId);\nsetP('x_eval.t7.seed',JSON.stringify({decoy:{id:dcId,m:dcr.sys_mod_count+''},role:roleId}));\ngs.print(JSON.stringify({seeded:true,role:'x_eval_h7_ops'}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t7.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar n=0;var acl=null;var q=new GlideRecord('sys_security_acl');q.addQuery('name','incident.urgency');q.addQuery('operation','write');q.query();while(q.next()){n++;if(n===1){acl=new GlideRecord('sys_security_acl');acl.get(q.sys_id+'');}}\nchk('exactly_one_acl',n===1);\nif(acl){chk('type_record',(acl.type+'')==='record');chk('acl_active',(acl.active+'')==='true');chk('admin_overrides_false',(acl.admin_overrides+'')==='false');\nvar roles=[];var rg=new GlideRecord('sys_security_acl_role');rg.addQuery('sys_security_acl',acl.sys_id+'');rg.query();while(rg.next()){var rr=new GlideRecord('sys_user_role');if(rr.get(rg.sys_user_role+''))roles.push(rr.name+'');}\nroles.sort();chk('exact_role_set',roles.join(',')==='itil,x_eval_h7_ops');}\nvar dcr=new GlideRecord('sys_security_acl');if(!dcr.get(seed.decoy.id)){bad.push('decoy_missing');}else{chk('decoy_untouched',(dcr.sys_mod_count+'')===seed.decoy.m);}\ngs.print(JSON.stringify({pass:bad.length===0,expected:'1 write ACL, roles={itil,x_eval_h7_ops}, ao=false',actual:bad.length?('FAIL:'+bad.join(',')):('ok')}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nfunction getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nfunction wipeAcl(nm,op){var a=new GlideRecord('sys_security_acl');a.addQuery('name',nm);if(op)a.addQuery('operation',op);a.query();while(a.next()){var rg=new GlideRecord('sys_security_acl_role');rg.addQuery('sys_security_acl',a.sys_id+'');rg.query();while(rg.next())rg.deleteRecord();a.deleteRecord();}}\nwipeAcl('incident.urgency','write');\ntry{var s=JSON.parse(getP('x_eval.t7.seed'));if(s&&s.decoy){var d=new GlideRecord('sys_security_acl');if(d.get(s.decoy.id)){var rg=new GlideRecord('sys_security_acl_role');rg.addQuery('sys_security_acl',s.decoy.id);rg.query();while(rg.next())rg.deleteRecord();d.deleteRecord();}}}catch(e){}\nvar da=new GlideRecord('sys_security_acl');da.addQuery('description','STARTSWITH','[EVAL-H7]');da.query();while(da.next())da.deleteRecord();\nvar orl=new GlideRecord('sys_user_role');orl.addQuery('name','STARTSWITH','x_eval_h7');orl.query();while(orl.next()){var hg=new GlideRecord('sys_user_has_role');hg.addQuery('role',orl.sys_id+'');hg.query();while(hg.next())hg.deleteRecord();orl.deleteRecord();}\ndelPfx('x_eval.t7.');delPfx('x_eval.h7.');"
    },
    {
      "id": "T8",
      "name": "Role containment + single grant",
      "category": "role",
      "points": 2,
      "prompt": "Create a role 'x_eval_h8_auditor' with description 'Eval H8 auditor role' that CONTAINS the existing role 'x_eval_h8_child' (role containment via sys_user_role_contains). Then grant 'x_eval_h8_auditor' to exactly one user: the user whose user_name is the value of the system property 'x_eval.h8.user'. Grant it to no one else.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar orl=new GlideRecord('sys_user_role');orl.addQuery('name','STARTSWITH','x_eval_h8_');orl.query();while(orl.next()){var id=orl.sys_id+'';var hg=new GlideRecord('sys_user_has_role');hg.addQuery('role',id);hg.query();while(hg.next())hg.deleteRecord();var c1=new GlideRecord('sys_user_role_contains');c1.addQuery('role',id);c1.query();while(c1.next())c1.deleteRecord();var c2=new GlideRecord('sys_user_role_contains');c2.addQuery('contains',id);c2.query();while(c2.next())c2.deleteRecord();orl.deleteRecord();}\nvar wu=new GlideRecord('sys_user');wu.addQuery('user_name','STARTSWITH','eval.h8.');wu.query();while(wu.next())wu.deleteRecord();\ndelPfx('x_eval.t8.');delPfx('x_eval.h8.');\nvar cr=new GlideRecord('sys_user_role');cr.initialize();cr.name='x_eval_h8_child';cr.description='Eval H8 child role';var childId=cr.insert()+'';\nfunction mkU(un,fn){var u=new GlideRecord('sys_user');u.initialize();u.user_name=un;u.first_name=fn;u.last_name='EvalTaylor';u.active=true;return u.insert()+'';}\nvar idA=mkU('eval.h8.taylor','Jordan');var idB=mkU('eval.h8.taylor2','Casey');\nvar pick=Math.random()<0.5;var target=pick?idA:idB;var other=pick?idB:idA;var uname=pick?'eval.h8.taylor':'eval.h8.taylor2';\nsetP('x_eval.h8.user',uname);\nsetP('x_eval.t8.seed',JSON.stringify({child:childId,target:target,other:other}));\ngs.print(JSON.stringify({seeded:true,props:['x_eval.h8.user']}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t8.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar n=0;var role=null;var q=new GlideRecord('sys_user_role');q.addQuery('name','x_eval_h8_auditor');q.query();while(q.next()){n++;if(n===1){role=new GlideRecord('sys_user_role');role.get(q.sys_id+'');}}\nchk('exactly_one_role',n===1);\nif(role){chk('description',(role.description+'')==='Eval H8 auditor role');\nvar cc=0;var cq=new GlideRecord('sys_user_role_contains');cq.addQuery('role',role.sys_id+'');cq.addQuery('contains',seed.child);cq.query();while(cq.next())cc++;chk('contains_child',cc===1);\nvar gts=[];var gq=new GlideRecord('sys_user_has_role');gq.addQuery('role',role.sys_id+'');gq.query();while(gq.next())gts.push(gq.user+'');\nchk('single_grant',gts.length===1&&gts[0]===seed.target);}\ngs.print(JSON.stringify({pass:bad.length===0,expected:'role+containment+1 grant to target user',actual:bad.length?('FAIL:'+bad.join(',')):('ok')}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar orl=new GlideRecord('sys_user_role');orl.addQuery('name','STARTSWITH','x_eval_h8_');orl.query();while(orl.next()){var id=orl.sys_id+'';var hg=new GlideRecord('sys_user_has_role');hg.addQuery('role',id);hg.query();while(hg.next())hg.deleteRecord();var c1=new GlideRecord('sys_user_role_contains');c1.addQuery('role',id);c1.query();while(c1.next())c1.deleteRecord();var c2=new GlideRecord('sys_user_role_contains');c2.addQuery('contains',id);c2.query();while(c2.next())c2.deleteRecord();orl.deleteRecord();}\nvar wu=new GlideRecord('sys_user');wu.addQuery('user_name','STARTSWITH','eval.h8.');wu.query();while(wu.next())wu.deleteRecord();\ndelPfx('x_eval.t8.');delPfx('x_eval.h8.');"
    },
    {
      "id": "T9",
      "name": "Widget with executed server script",
      "category": "widget",
      "points": 4,
      "prompt": "Create a Service Portal widget (sp_widget) with id='eval-h9-widget' and name='Eval H9 Widget'. Its server script must use GlideAggregate to set data.count to the NUMBER of currently active priority-1 incidents (count of incidents where active=true AND priority=1, assigned as a numeric value). Its template must contain an <h1> element whose content is 'H9 {{data.count}}'. Its CSS must contain a rule for the class '.eval-h9' that sets color: red.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nfunction cAgg(act,pri){var ga=new GlideAggregate('incident');ga.addAggregate('COUNT');if(act!==null)ga.addQuery('active',act);if(pri!==null)ga.addQuery('priority',pri);ga.query();return ga.next()?parseInt(ga.getAggregate('COUNT'),10):0;}\nfunction counts(){return{a:cAgg(true,1),b:cAgg(null,1),c:cAgg(true,null)};}\nvar w=new GlideRecord('sp_widget');if(w.get('id','eval-h9-widget'))w.deleteRecord();\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H9');wq.query();while(wq.next())wq.deleteRecord();\ndelPfx('x_eval.t9.');delPfx('x_eval.h9.');\nfunction mkI(d,im,ur,close){var n=new GlideRecord('incident');n.initialize();n.short_description=d;n.impact=im;n.urgency=ur;if(close){n.state=7;n.active=false;n.close_code='Closed/Resolved by Caller';n.close_notes='eval h9 seed';}n.insert();}\nmkI('[EVAL-H9] seed A',1,1,false);mkI('[EVAL-H9] seed B',1,1,false);\nvar t=0;var x=counts();\nwhile(t<6&&(x.b===x.a||x.c===x.a||x.c===x.b)){if(x.b===x.a){mkI('[EVAL-H9] filler closed P1 '+t,1,1,true);}else{mkI('[EVAL-H9] filler active P3 '+t,2,2,false);}x=counts();t++;}\nsetP('x_eval.t9.seed',JSON.stringify({a:x.a,b:x.b,c:x.c}));\ngs.print(JSON.stringify({seeded:true,widget_id:'eval-h9-widget'}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t9.seed'))||null;}catch(e){return null;}}\nfunction cAgg(act,pri){var ga=new GlideAggregate('incident');ga.addAggregate('COUNT');if(act!==null)ga.addQuery('active',act);if(pri!==null)ga.addQuery('priority',pri);ga.query();return ga.next()?parseInt(ga.getAggregate('COUNT'),10):0;}\nfunction counts(){return{a:cAgg(true,1),b:cAgg(null,1),c:cAgg(true,null)};}\nvar pids=[];\nfunction mkP(d,im,ur,close){var n=new GlideRecord('incident');n.initialize();n.short_description=d;n.impact=im;n.urgency=ur;if(close){n.state=7;n.active=false;n.close_code='Closed/Resolved by Caller';n.close_notes='eval h9 probe';}pids.push(n.insert()+'');}\nfunction delProbes(){for(var x9=0;x9<pids.length;x9++){try{var dr=new GlideRecord('incident');if(dr.get(pids[x9]))dr.deleteRecord();}catch(ed){}}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nmkP('[EVAL-H9] probe P1',1,1,false);\nvar x=counts();var t=0;\nwhile(t<5&&(x.b===x.a||x.c===x.a||x.c===x.b)){if(x.b===x.a){mkP('[EVAL-H9] probe fill closed '+t,1,1,true);}else{mkP('[EVAL-H9] probe fill P3 '+t,2,2,false);}x=counts();t++;}\nchk('counts_distinct',x.b!==x.a&&x.c!==x.a&&x.c!==x.b);\nvar w=new GlideRecord('sp_widget');\nif(!w.get('id','eval-h9-widget')){bad.push('widget_missing');}else{\nchk('widget_name',(w.name+'')==='Eval H9 Widget');\nvar stext=w.getValue('script')||'';var tpl=w.getValue('template')||'';var css=w.getValue('css')||'';\nchk('uses_glideaggregate',stext.indexOf('GlideAggregate')!==-1);\nvar dataObj={};var execErr='';\ntry{(function(data,input,options){eval(stext);})(dataObj,{},{});}catch(e2){execErr=String(e2).substring(0,40);}\nif(execErr!==''){bad.push('exec:'+execErr);}else{\nchk('count_is_number',typeof dataObj.count==='number');\nchk('count_value',dataObj.count===x.a);}\nvar hm=tpl.match(/<h1[\\s\\S]*?<\\/h1>/i);\nchk('template_h1',!!hm&&hm[0].indexOf('{{data.count}}')!==-1&&hm[0].indexOf('H9')!==-1);\nchk('css_red',/\\.eval-h9\\s*\\{[^}]*color\\s*:\\s*red/i.test(css));}\ndelProbes();\ngs.print(JSON.stringify({pass:bad.length===0,expected:'script eval count='+x.a+' + tpl/css',actual:bad.length?('FAIL:'+bad.join(',')):('ok')}));\n}\n}catch(e){try{delProbes();}catch(e9){}gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar w=new GlideRecord('sp_widget');if(w.get('id','eval-h9-widget'))w.deleteRecord();\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H9');wq.query();while(wq.next())wq.deleteRecord();\ndelPfx('x_eval.t9.');delPfx('x_eval.h9.');"
    },
    {
      "id": "T10",
      "name": "Attachment with derived JSON content",
      "category": "attachment",
      "points": 4,
      "prompt": "Find the incident whose short_description is '[EVAL-H10] Needs attachment'. Attach to it a file named 'eval_h10.txt' with content_type 'text/plain' whose ENTIRE content is exactly this minified JSON (no spaces, no trailing newline): {\"token\":\"<T>\",\"open\":<N>} — where <T> is the value of the system property 'x_eval.h10.token' and <N> is the count of currently active incidents whose short_description starts with '[EVAL-H10]'. Do not delete or modify any existing attachment.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wi=new GlideRecord('incident');wi.addQuery('short_description','STARTSWITH','[EVAL-H10');wi.query();while(wi.next()){var oa=new GlideRecord('sys_attachment');oa.addQuery('table_name','incident');oa.addQuery('table_sys_id',wi.sys_id+'');oa.query();while(oa.next())oa.deleteRecord();wi.deleteRecord();}\ndelPfx('x_eval.t10.');delPfx('x_eval.h10.');\nvar tok=gs.generateGUID().substring(0,8);\nvar n=new GlideRecord('incident');n.initialize();n.short_description='[EVAL-H10] Needs attachment';n.impact=2;n.urgency=2;var tid=n.insert()+'';\nvar tg=new GlideRecord('incident');tg.get(tid);\nvar keepBody='This attachment must survive the eval run. '+gs.generateGUID();\nvar gsa=new GlideSysAttachment();var keepId=gsa.write(tg,'keep_me.txt','text/plain',keepBody)+'';\nvar nx=1+Math.floor(Math.random()*4);\nfor(var i=0;i<nx;i++){var e=new GlideRecord('incident');e.initialize();e.short_description='[EVAL-H10] Open item '+i;e.impact=2;e.urgency=2;e.insert();}\nsetP('x_eval.h10.token',tok);\nsetP('x_eval.t10.seed',JSON.stringify({tok:tok,target:tid,keep:{id:keepId,size:String(keepBody.length)}}));\ngs.print(JSON.stringify({seeded:true,props:['x_eval.h10.token']}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t10.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar nOpen=0;var oq=new GlideRecord('incident');oq.addQuery('short_description','STARTSWITH','[EVAL-H10]');oq.addQuery('active',true);oq.query();while(oq.next())nOpen++;\nvar exp='{\"token\":\"'+seed.tok+'\",\"open\":'+nOpen+'}';\nvar n=0;var att=null;var aq=new GlideRecord('sys_attachment');aq.addQuery('table_name','incident');aq.addQuery('table_sys_id',seed.target);aq.addQuery('file_name','eval_h10.txt');aq.query();while(aq.next()){n++;if(n===1){att=new GlideRecord('sys_attachment');att.get(aq.sys_id+'');}}\nchk('exactly_one_attachment',n===1);\nif(att){chk('content_type',(att.content_type+'').indexOf('text/plain')===0);\nchk('byte_size',parseInt(att.size_bytes+'',10)===exp.length);\nvar content='';try{var st=new GlideSysAttachment().getContentStream(att.sys_id+'');var rd=new GlideTextReader(st);var ln;var outl=[];while((ln=rd.readLine())!==null)outl.push(ln);content=outl.join('\\n');}catch(e3){bad.push('read:'+String(e3).substring(0,30));}\nchk('content_exact',content===exp);}\nvar kp=new GlideRecord('sys_attachment');if(!kp.get(seed.keep.id)){bad.push('keep_me_missing');}else{chk('keep_me_size',(kp.size_bytes+'')===seed.keep.size);}\ngs.print(JSON.stringify({pass:bad.length===0,expected:'eval_h10.txt == derived JSON, keep_me intact',actual:bad.length?('FAIL:'+bad.join(',')):('ok open='+nOpen)}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wi=new GlideRecord('incident');wi.addQuery('short_description','STARTSWITH','[EVAL-H10');wi.query();while(wi.next()){var oa=new GlideRecord('sys_attachment');oa.addQuery('table_name','incident');oa.addQuery('table_sys_id',wi.sys_id+'');oa.query();while(oa.next())oa.deleteRecord();wi.deleteRecord();}\ndelPfx('x_eval.t10.');delPfx('x_eval.h10.');"
    },
    {
      "id": "T11",
      "name": "Correlate split syslog secret",
      "category": "logs",
      "points": 4,
      "prompt": "The source 'eval-h11-secret' wrote two entries to the system log (syslog table): one containing 'part1=' followed by an 8-character value, and one containing 'part2=' followed by an 8-character value. Find both, concatenate the two values as part1-value then part2-value (16 characters, no separator), and write the result to the system property 'x_eval.h11.result'. Beware: other eval sources also logged fake 'part1=' lines.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wl=new GlideRecord('syslog');wl.addQuery('source','STARTSWITH','eval-h11');wl.query();while(wl.next())wl.deleteRecord();\ndelPfx('x_eval.t11.');delPfx('x_eval.h11.');\nvar p1=gs.generateGUID().substring(0,8);var p2=gs.generateGUID().substring(0,8);\nvar dg=gs.generateGUID();var d1=dg.substring(0,8);var d2=dg.substring(8,16);\nvar srcs=['eval-h11-app','eval-h11-worker','eval-h11-noise'];\nvar firstIsP1=Math.random()<0.5;\nfor(var i=0;i<120;i++){\ngs.log('routine sync line '+i+' status ok latency '+(10+i%37)+'ms',srcs[i%3]);\nif(i===30)gs.log('diagnostic dump part1='+d1+' checksum stale','eval-h11-noise');\nif(i===45)gs.log('trace segment '+(firstIsP1?('part1='+p1):('part2='+p2))+' recorded','eval-h11-secret');\nif(i===72)gs.log('diagnostic dump part1='+d2+' checksum stale','eval-h11-noise');\nif(i===95)gs.log('trace segment '+(firstIsP1?('part2='+p2):('part1='+p1))+' recorded','eval-h11-secret');\n}\nsetP('x_eval.t11.seed',JSON.stringify({ans:p1+p2}));\ngs.print(JSON.stringify({seeded:true,source:'eval-h11-secret'}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t11.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar got=getP('x_eval.h11.result');\nchk('secret',got===seed.ans);\ngs.print(JSON.stringify({pass:bad.length===0,expected:seed.ans,actual:bad.length?('FAIL:'+bad.join(',')):(got)}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wl=new GlideRecord('syslog');wl.addQuery('source','STARTSWITH','eval-h11');wl.query();while(wl.next())wl.deleteRecord();\ndelPfx('x_eval.t11.');delPfx('x_eval.h11.');"
    },
    {
      "id": "T12",
      "name": "Selective email queue transition",
      "category": "queue",
      "points": 3,
      "prompt": "In the email queue (sys_email), mark as sent — set type='sent' — ONLY the messages that are currently type 'send-ready' AND whose subject starts with '[EVAL-H12] ' (tag + one space). Do not touch received, already-sent, or any other messages. Write the count of messages you transitioned, as a string, to the system property 'x_eval.h12.result'.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar we=new GlideRecord('sys_email');we.addQuery('subject','STARTSWITH','[EVAL-H12');we.query();while(we.next())we.deleteRecord();\ndelPfx('x_eval.t12.');delPfx('x_eval.h12.');\nvar g=gs.generateGUID();\nfunction mkE(sub,ty){var e=new GlideRecord('sys_email');e.initialize();e.subject=sub;e.body='eval h12 body';e.recipients='noone@example.com';e.type=ty;var id=e.insert()+'';var r=new GlideRecord('sys_email');r.get(id);return{id:id,m:r.sys_mod_count+'',t:ty};}\nvar el=[];var nel=3+Math.floor(Math.random()*4);\nfor(var i=0;i<nel;i++)el.push(mkE('[EVAL-H12] Digest '+i+' '+g.substring(0,6),'send-ready').id);\nvar traps=[];\ntraps.push(mkE('[EVAL-H12] Inbound reply '+g.substring(6,12),'received'));\ntraps.push(mkE('[EVAL-H12] Inbound notice '+g.substring(12,18),'received'));\ntraps.push(mkE('[EVAL-H12] Sent earlier '+g.substring(18,24),'sent'));\ntraps.push(mkE('[EVAL-H12X] Digest '+g.substring(24,30),'send-ready'));\nsetP('x_eval.t12.seed',JSON.stringify({el:el,n:nel,traps:traps}));\ngs.print(JSON.stringify({seeded:true,marker:'[EVAL-H12] '}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t12.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nfor(var i=0;i<seed.el.length;i++){var r=new GlideRecord('sys_email');if(!r.get(seed.el[i])){bad.push('elmissing'+i);continue;}if((r.type+'')!=='sent')bad.push('el'+i);}\nfor(var j=0;j<seed.traps.length;j++){var tp=seed.traps[j];var t=new GlideRecord('sys_email');if(!t.get(tp.id)){bad.push('trapmissing'+j);continue;}if((t.type+'')!==tp.t||(t.sys_mod_count+'')!==tp.m)bad.push('trap'+j);}\nchk('count_prop',getP('x_eval.h12.result')===String(seed.n));\ngs.print(JSON.stringify({pass:bad.length===0,expected:seed.n+' transitioned, traps untouched',actual:bad.length?('FAIL:'+bad.join(',')):('ok n='+seed.n)}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar we=new GlideRecord('sys_email');we.addQuery('subject','STARTSWITH','[EVAL-H12');we.query();while(we.next())we.deleteRecord();\ndelPfx('x_eval.t12.');delPfx('x_eval.h12.');"
    },
    {
      "id": "T13",
      "name": "UI Action with derived action_name",
      "category": "ui_action",
      "points": 2,
      "prompt": "Create a UI Action (sys_ui_action) on the 'incident' table with: name='Eval H13 Action', action_name='eval_h13_' followed by the value of the system property 'x_eval.h13.token', show_update=true, show_insert=false, form_button=true, client=false, active=true, order=250, condition exactly 'current.state != 7', and a script that calls gs.addInfoMessage and contains the marker '[EVAL-H13]'.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wa=new GlideRecord('sys_ui_action');wa.addQuery('action_name','STARTSWITH','eval_h13_');wa.query();while(wa.next())wa.deleteRecord();\nvar wn=new GlideRecord('sys_ui_action');wn.addQuery('name','Eval H13 Action');wn.query();while(wn.next())wn.deleteRecord();\ndelPfx('x_eval.t13.');delPfx('x_eval.h13.');\nvar tok=gs.generateGUID().substring(0,6);\nsetP('x_eval.h13.token',tok);\nsetP('x_eval.t13.seed',JSON.stringify({tok:tok}));\ngs.print(JSON.stringify({seeded:true,props:['x_eval.h13.token']}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t13.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar an='eval_h13_'+seed.tok;\nvar n=0;var u=null;var q=new GlideRecord('sys_ui_action');q.addQuery('action_name',an);q.query();while(q.next()){n++;if(n===1){u=new GlideRecord('sys_ui_action');u.get(q.sys_id+'');}}\nchk('exactly_one',n===1);\nif(u){chk('table',(u.table+'')==='incident');chk('name',(u.name+'')==='Eval H13 Action');\nchk('show_update',(u.show_update+'')==='true');chk('show_insert',(u.show_insert+'')==='false');\nchk('form_button',(u.form_button+'')==='true');chk('client',(u.client+'')==='false');chk('active',(u.active+'')==='true');\nchk('order',(u.getValue('order')+'')==='250');chk('condition',(u.condition+'')==='current.state != 7');\nvar sc=u.getValue('script')||'';chk('script_addinfo',sc.indexOf('gs.addInfoMessage')!==-1);chk('script_marker',sc.indexOf('[EVAL-H13]')!==-1);}\ngs.print(JSON.stringify({pass:bad.length===0,expected:'action_name='+an+' all fields',actual:bad.length?('FAIL:'+bad.join(',')):('ok')}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wa=new GlideRecord('sys_ui_action');wa.addQuery('action_name','STARTSWITH','eval_h13_');wa.query();while(wa.next())wa.deleteRecord();\nvar wn=new GlideRecord('sys_ui_action');wn.addQuery('name','Eval H13 Action');wn.query();while(wn.next())wn.deleteRecord();\ndelPfx('x_eval.t13.');delPfx('x_eval.h13.');"
    },
    {
      "id": "T14",
      "name": "onChange client script on derived field",
      "category": "client_script",
      "points": 3,
      "prompt": "Create a client script (sys_script_client) on the 'incident' table: name='Eval H14 Client Script', type='onChange', and the monitored field (element 'field') must be the field named by the value of the system property 'x_eval.h14.field'. Also set ui_type=10 (All), order=150, isolate_script=false, applies_extended=false, active=true. The script body must contain the marker '[EVAL-H14]'.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wc=new GlideRecord('sys_script_client');wc.addQuery('name','Eval H14 Client Script');wc.query();while(wc.next())wc.deleteRecord();\ndelPfx('x_eval.t14.');delPfx('x_eval.h14.');\nvar fields=['impact','urgency','state'];var f=fields[Math.floor(Math.random()*3)];\nsetP('x_eval.h14.field',f);\nsetP('x_eval.t14.seed',JSON.stringify({f:f}));\ngs.print(JSON.stringify({seeded:true,props:['x_eval.h14.field']}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t14.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar n=0;var c=null;var q=new GlideRecord('sys_script_client');q.addQuery('name','Eval H14 Client Script');q.query();while(q.next()){n++;if(n===1){c=new GlideRecord('sys_script_client');c.get(q.sys_id+'');}}\nchk('exactly_one',n===1);\nif(c){chk('table',(c.table+'')==='incident');chk('type',(c.type+'')==='onChange');\nchk('field',(c.getValue('field')+'')===seed.f);\nchk('ui_type',(c.getValue('ui_type')+'')==='10');chk('order',(c.getValue('order')+'')==='150');\nchk('isolate_script',(c.isolate_script+'')==='false');chk('applies_extended',(c.applies_extended+'')==='false');\nchk('active',(c.active+'')==='true');\nchk('marker',(c.getValue('script')||'').indexOf('[EVAL-H14]')!==-1);}\ngs.print(JSON.stringify({pass:bad.length===0,expected:'onChange on '+seed.f+' ui_type=10 order=150',actual:bad.length?('FAIL:'+bad.join(',')):('ok')}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wc=new GlideRecord('sys_script_client');wc.addQuery('name','Eval H14 Client Script');wc.query();while(wc.next())wc.deleteRecord();\ndelPfx('x_eval.t14.');delPfx('x_eval.h14.');"
    },
    {
      "id": "T15",
      "name": "Event registration + notification",
      "category": "notification",
      "points": 3,
      "prompt": "Two artifacts: (1) Register an event — insert a sysevent_register record whose event_name is the value of the system property 'x_eval.h15.event_name', for the 'incident' table. (2) Create an email notification (sysevent_email_action) named 'Eval H15 Notification' on collection 'incident', active=true, triggered by that same event (its event_name field must be that event), whose subject contains the value of the system property 'x_eval.h15.token', whose message contains 'H15-BODY-' followed by that token, and whose recipients include the group named '[EVAL-H15] Group'.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wn=new GlideRecord('sysevent_email_action');wn.addQuery('name','Eval H15 Notification');wn.query();while(wn.next())wn.deleteRecord();\nvar wr=new GlideRecord('sysevent_register');wr.addQuery('event_name','STARTSWITH','x_eval.h15.evt_');wr.query();while(wr.next())wr.deleteRecord();\nvar wg=new GlideRecord('sys_user_group');wg.addQuery('name','STARTSWITH','[EVAL-H15]');wg.query();while(wg.next())wg.deleteRecord();\ndelPfx('x_eval.t15.');delPfx('x_eval.h15.');\nvar tok=gs.generateGUID().substring(0,8);var evt='x_eval.h15.evt_'+gs.generateGUID().substring(0,6);\nvar gp=new GlideRecord('sys_user_group');gp.initialize();gp.name='[EVAL-H15] Group';gp.description='Eval H15 recipients';var gid=gp.insert()+'';\nsetP('x_eval.h15.token',tok);setP('x_eval.h15.event_name',evt);\nsetP('x_eval.t15.seed',JSON.stringify({tok:tok,evt:evt,gid:gid}));\ngs.print(JSON.stringify({seeded:true,props:['x_eval.h15.token','x_eval.h15.event_name']}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t15.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar nr=0;var reg=null;var rq=new GlideRecord('sysevent_register');rq.addQuery('event_name',seed.evt);rq.query();while(rq.next()){nr++;if(nr===1){reg=new GlideRecord('sysevent_register');reg.get(rq.sys_id+'');}}\nchk('registration_exists',nr===1);\nif(reg)chk('registration_table',(reg.table+'')==='incident');\nvar nn=0;var no=null;var nq=new GlideRecord('sysevent_email_action');nq.addQuery('name','Eval H15 Notification');nq.query();while(nq.next()){nn++;if(nn===1){no=new GlideRecord('sysevent_email_action');no.get(nq.sys_id+'');}}\nchk('exactly_one_notification',nn===1);\nif(no){chk('collection',(no.collection+'')==='incident');chk('active',(no.active+'')==='true');\nchk('event_wired',(no.event_name+'')===seed.evt);\nchk('subject_token',(no.subject+'').indexOf(seed.tok)!==-1);\nvar body=(no.message+'')+'\\n'+(no.message_html+'')+'\\n'+(no.message_text+'');\nchk('body_token',body.indexOf('H15-BODY-'+seed.tok)!==-1);\nchk('group_recipient',(no.getValue('recipient_groups')||'').indexOf(seed.gid)!==-1);}\ngs.print(JSON.stringify({pass:bad.length===0,expected:'register+notification wired to '+seed.evt,actual:bad.length?('FAIL:'+bad.join(',')):('ok')}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nfunction getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar wn=new GlideRecord('sysevent_email_action');wn.addQuery('name','Eval H15 Notification');wn.query();while(wn.next())wn.deleteRecord();\nvar wr=new GlideRecord('sysevent_register');wr.addQuery('event_name','STARTSWITH','x_eval.h15.evt_');wr.query();while(wr.next())wr.deleteRecord();\ntry{var s=JSON.parse(getP('x_eval.t15.seed'));if(s&&s.gid){var g1=new GlideRecord('sys_user_group');if(g1.get(s.gid))g1.deleteRecord();}}catch(e){}\nvar wg=new GlideRecord('sys_user_group');wg.addQuery('name','STARTSWITH','[EVAL-H15]');wg.query();while(wg.next())wg.deleteRecord();\ndelPfx('x_eval.t15.');delPfx('x_eval.h15.');"
    },
    {
      "id": "T16",
      "name": "Threshold aggregate by category",
      "category": "aggregate",
      "points": 5,
      "prompt": "Consider only incidents whose short_description starts with '[EVAL-H16]'. Aggregate the ACTIVE ones by category and report ONLY the categories that have at least 3 active matching incidents. Write the result to the system property 'x_eval.h16.result' as a JSON object mapping each qualifying category value to its active count, with NUMERIC values (e.g. {\"hardware\":4}) and no other keys. Do not create, modify, or delete any incident.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H16');wq.query();while(wq.next())wq.deleteRecord();\ndelPfx('x_eval.t16.');delPfx('x_eval.h16.');\nvar cats=['hardware','software','network','database','inquiry'];\nvar recs=[];var k=0;\nfunction mk16(cat,close){var n=new GlideRecord('incident');n.initialize();n.short_description='[EVAL-H16] item '+(k++);n.impact=2;n.urgency=2;n.category=cat;if(close){n.state=7;n.active=false;n.close_code='Closed/Resolved by Caller';n.close_notes='eval h16 seed';}var id=n.insert()+'';var r=new GlideRecord('incident');r.get(id);recs.push({id:id,m:r.sys_mod_count+''});}\nvar forced=cats[Math.floor(Math.random()*cats.length)];\nfor(var ci=0;ci<cats.length;ci++){\nvar na=(cats[ci]===forced)?(3+Math.floor(Math.random()*3)):Math.floor(Math.random()*6);\nvar ni=Math.floor(Math.random()*4);\nfor(var a=0;a<na;a++)mk16(cats[ci],false);\nfor(var b=0;b<ni;b++)mk16(cats[ci],true);\n}\nsetP('x_eval.t16.seed',JSON.stringify({recs:recs,pop:recs.length}));\ngs.print(JSON.stringify({seeded:true,marker:'[EVAL-H16] '}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t16.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar tot=0;var m={};\nvar q=new GlideRecord('incident');q.addQuery('short_description','STARTSWITH','[EVAL-H16');q.query();\nwhile(q.next()){tot++;if((q.active+'')==='true'){var c=q.category+'';m[c]=(m[c]||0)+1;}}\nvar qual={};for(var c1 in m)if(m[c1]>=3)qual[c1]=m[c1];\nchk('population',tot===seed.pop);\nfor(var i=0;i<seed.recs.length;i++){var rc=seed.recs[i];var r=new GlideRecord('incident');if(!r.get(rc.id)){bad.push('missing'+i);continue;}if((r.sys_mod_count+'')!==rc.m)bad.push('touched'+i);}\nvar raw=getP('x_eval.h16.result');var obj=null;\ntry{obj=JSON.parse(raw);}catch(e2){}\nif(!obj||typeof obj!=='object'||Object.prototype.toString.call(obj)==='[object Array]'){bad.push('unparseable');}else{\nfor(var k1 in qual)if(obj[k1]!==qual[k1])bad.push('wrong_'+k1);\nfor(var k2 in obj)if(!qual.hasOwnProperty(k2))bad.push('extra_'+k2);}\ngs.print(JSON.stringify({pass:bad.length===0,expected:JSON.stringify(qual),actual:bad.length?('FAIL:'+bad.join(',')):(raw)}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H16');wq.query();while(wq.next())wq.deleteRecord();\ndelPfx('x_eval.t16.');delPfx('x_eval.h16.');"
    },
    {
      "id": "T17",
      "name": "Dependent choice insert + retire",
      "category": "choice",
      "points": 2,
      "prompt": "On the 'incident' table's 'subcategory' field (sys_choice, language 'en'): (1) insert a new choice with value='eval_h17', label='[EVAL-H17] Choice', dependent_value='hardware', inactive=false, sequence=990; and (2) set inactive=true on the EXISTING choice with value='eval_h17_retire' (do not delete it). Change nothing else on this choice list.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wc=new GlideRecord('sys_choice');wc.addQuery('name','incident');wc.addQuery('element','subcategory');wc.addQuery('value','STARTSWITH','eval_h17');wc.query();while(wc.next())wc.deleteRecord();\ndelPfx('x_eval.t17.');delPfx('x_eval.h17.');\nvar r=new GlideRecord('sys_choice');r.initialize();r.name='incident';r.element='subcategory';r.value='eval_h17_retire';r.label='[EVAL-H17] Retiring';r.dependent_value='hardware';r.language='en';r.inactive=false;r.sequence=980;var rid=r.insert()+'';\nvar others=[];var oq=new GlideRecord('sys_choice');oq.addQuery('name','incident');oq.addQuery('element','subcategory');oq.addQuery('language','en');oq.query();\nwhile(oq.next()){var v=oq.value+'';if(v.indexOf('eval_h17')===0)continue;others.push(v+':'+oq.dependent_value+':'+oq.inactive);}\nothers.sort();\nsetP('x_eval.t17.seed',JSON.stringify({rid:rid,others:others.join('|')}));\ngs.print(JSON.stringify({seeded:true,retire_value:'eval_h17_retire'}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t17.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar n=0;var ch=null;var q=new GlideRecord('sys_choice');q.addQuery('name','incident');q.addQuery('element','subcategory');q.addQuery('value','eval_h17');q.query();while(q.next()){n++;if(n===1){ch=new GlideRecord('sys_choice');ch.get(q.sys_id+'');}}\nchk('exactly_one_new',n===1);\nif(ch){chk('label',(ch.label+'')==='[EVAL-H17] Choice');chk('dependent',(ch.dependent_value+'')==='hardware');\nchk('language',(ch.language+'')==='en');chk('inactive_false',(ch.inactive+'')==='false');chk('sequence',(ch.getValue('sequence')+'')==='990');}\nvar rt=new GlideRecord('sys_choice');if(!rt.get(seed.rid)){bad.push('retire_missing');}else{chk('retired',(rt.inactive+'')==='true');chk('retire_value',(rt.value+'')==='eval_h17_retire');}\nvar others=[];var oq=new GlideRecord('sys_choice');oq.addQuery('name','incident');oq.addQuery('element','subcategory');oq.addQuery('language','en');oq.query();\nwhile(oq.next()){var v=oq.value+'';if(v.indexOf('eval_h17')===0)continue;others.push(v+':'+oq.dependent_value+':'+oq.inactive);}\nothers.sort();chk('siblings_untouched',others.join('|')===seed.others);\ngs.print(JSON.stringify({pass:bad.length===0,expected:'new choice + retired + siblings intact',actual:bad.length?('FAIL:'+bad.join(',')):('ok')}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wc=new GlideRecord('sys_choice');wc.addQuery('name','incident');wc.addQuery('element','subcategory');wc.addQuery('value','STARTSWITH','eval_h17');wc.query();while(wc.next())wc.deleteRecord();\ndelPfx('x_eval.t17.');delPfx('x_eval.h17.');"
    },
    {
      "id": "T18",
      "name": "Custom table extending task (strict)",
      "category": "table",
      "points": 5,
      "prompt": "Create a custom table named 'u_eval_h18' that EXTENDS the 'task' table, with label 'Eval H18 Task', in the GLOBAL scope (sys_scope must be global — beware: table-creation APIs inherit the session's current application scope). Give it exactly ONE custom column: element 'u_marker', column label 'Marker', type string (internal_type='string'), max_length=40. It must have no other custom (u_*) columns.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\ntry{var tu=new TableUtils('u_eval_h18');if(tu.tableExists())tu.drop();}catch(e){}\nvar t=new GlideRecord('sys_db_object');if(t.get('name','u_eval_h18'))t.deleteRecord();\nvar d=new GlideRecord('sys_dictionary');d.addQuery('name','u_eval_h18');d.query();while(d.next())d.deleteRecord();\ndelPfx('x_eval.t18.');delPfx('x_eval.h18.');\ngs.print(JSON.stringify({seeded:true,table:'u_eval_h18'}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t18.seed'))||null;}catch(e){return null;}}\ntry{\nvar n=0;var t=null;var q=new GlideRecord('sys_db_object');q.addQuery('name','u_eval_h18');q.query();while(q.next()){n++;if(n===1){t=new GlideRecord('sys_db_object');t.get(q.sys_id+'');}}\nchk('exactly_one_table',n===1);\nif(t){\nvar superName='';var sup=new GlideRecord('sys_db_object');if((t.super_class+'')&&sup.get(t.super_class+''))superName=sup.name+'';\nchk('extends_task',superName==='task');\nchk('label',(t.label+'')==='Eval H18 Task');\nchk('global_scope',(t.getValue('sys_scope')+'')==='global');\nvar nf=0;var f=null;var fq=new GlideRecord('sys_dictionary');fq.addQuery('name','u_eval_h18');fq.addQuery('element','u_marker');fq.query();while(fq.next()){nf++;if(nf===1){f=new GlideRecord('sys_dictionary');f.get(fq.sys_id+'');}}\nchk('u_marker_exists',nf===1);\nif(f){chk('type_string',(f.internal_type+'')==='string');chk('max_length_40',(f.getValue('max_length')+'')==='40');chk('column_label',(f.column_label+'')==='Marker');}\nvar nc=0;var cq=new GlideRecord('sys_dictionary');cq.addQuery('name','u_eval_h18');cq.addQuery('element','STARTSWITH','u_');cq.query();while(cq.next())nc++;\nchk('exactly_one_custom_column',nc===1);\n}\ngs.print(JSON.stringify({pass:bad.length===0,expected:'u_eval_h18 extends task, global, u_marker(40)',actual:bad.length?('FAIL:'+bad.join(',')):('ok')}));\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\ntry{var tu=new TableUtils('u_eval_h18');if(tu.tableExists())tu.drop();}catch(e){}\nvar t=new GlideRecord('sys_db_object');t.addQuery('name','u_eval_h18');t.query();while(t.next())t.deleteRecord();\nvar d=new GlideRecord('sys_dictionary');d.addQuery('name','u_eval_h18');d.query();while(d.next())d.deleteRecord();\ndelPfx('x_eval.t18.');delPfx('x_eval.h18.');"
    },
    {
      "id": "T19",
      "name": "Inactive daily scheduled job",
      "category": "scheduled_job",
      "points": 2,
      "prompt": "Create a scheduled script job (sysauto_script) named 'Eval H19 Job' with active=false (it must NOT be left scheduled to run) and run_type='daily'. Its script body must reference the property NAME stored in the system property 'x_eval.h19.flag' — i.e. read that property's value and include that exact text in the job's script (for example as the argument to gs.getProperty).",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar ws=new GlideRecord('sysauto_script');ws.addQuery('name','Eval H19 Job');ws.query();while(ws.next())ws.deleteRecord();\ndelPfx('x_eval.t19.');delPfx('x_eval.h19.');\nvar ref='x_eval.h19.ref_'+gs.generateGUID().substring(0,6);\nsetP('x_eval.h19.flag',ref);\nsetP('x_eval.t19.seed',JSON.stringify({ref:ref}));\ngs.print(JSON.stringify({seeded:true,props:['x_eval.h19.flag']}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t19.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar n=0;var s=null;var q=new GlideRecord('sysauto_script');q.addQuery('name','Eval H19 Job');q.query();while(q.next()){n++;if(n===1){s=new GlideRecord('sysauto_script');s.get(q.sys_id+'');}}\nchk('exactly_one',n===1);\nif(s){chk('inactive',(s.active+'')==='false');chk('run_type_daily',(s.getValue('run_type')+'')==='daily');\nchk('script_references_prop',(s.getValue('script')||'').indexOf(seed.ref)!==-1);}\ngs.print(JSON.stringify({pass:bad.length===0,expected:'inactive daily job referencing '+seed.ref,actual:bad.length?('FAIL:'+bad.join(',')):('ok')}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar ws=new GlideRecord('sysauto_script');ws.addQuery('name','Eval H19 Job');ws.query();while(ws.next())ws.deleteRecord();\ndelPfx('x_eval.t19.');delPfx('x_eval.h19.');"
    },
    {
      "id": "T20",
      "name": "Three-hop dot-walk count",
      "category": "dot_walk",
      "points": 4,
      "prompt": "Count the ACTIVE incidents whose caller's department's company is named exactly '[EVAL-H20] Corp'. Write the count as a string to the system property 'x_eval.h20.result'. Do not create, modify, or delete any record.",
      "setup_script": "function setP(n,v){var pp=new GlideRecord('sys_properties');if(pp.get('name',n)){pp.value=v;pp.update();}else{pp.initialize();pp.name=n;pp.value=v;pp.insert();}}\nfunction delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H20');wq.query();while(wq.next())wq.deleteRecord();\nvar wu=new GlideRecord('sys_user');wu.addQuery('user_name','STARTSWITH','eval.h20.');wu.query();while(wu.next())wu.deleteRecord();\nvar wd=new GlideRecord('cmn_department');wd.addQuery('name','STARTSWITH','[EVAL-H20]');wd.query();while(wd.next())wd.deleteRecord();\nvar wco=new GlideRecord('core_company');wco.addQuery('name','STARTSWITH','[EVAL-H20]');wco.query();while(wco.next())wco.deleteRecord();\ndelPfx('x_eval.t20.');delPfx('x_eval.h20.');\nfunction mkCo(nm){var c=new GlideRecord('core_company');c.initialize();c.name=nm;return c.insert()+'';}\nvar c1=mkCo('[EVAL-H20] Corp');var c2=mkCo('[EVAL-H20] Corp Holdings');\nfunction mkDep(nm,co){var d=new GlideRecord('cmn_department');d.initialize();d.name=nm;if(co)d.company=co;return d.insert()+'';}\nvar dA=mkDep('[EVAL-H20] Dept A',c1);var dB=mkDep('[EVAL-H20] Dept B',c2);var dC=mkDep('[EVAL-H20] Dept C',null);\nfunction mkU(un,dep){var u=new GlideRecord('sys_user');u.initialize();u.user_name=un;u.first_name='Eval';u.last_name='H20 '+un;if(dep)u.department=dep;u.active=true;return u.insert()+'';}\nvar uA=mkU('eval.h20.ua',dA);var uB=mkU('eval.h20.ub',dB);var uC=mkU('eval.h20.uc',dC);var uD=mkU('eval.h20.ud',null);\nvar recs=[];var k=0;\nfunction mkI(caller,close){var n=new GlideRecord('incident');n.initialize();n.short_description='[EVAL-H20] Case '+(k++);n.impact=2;n.urgency=2;if(caller)n.caller_id=caller;if(close){n.state=7;n.active=false;n.close_code='Closed/Resolved by Caller';n.close_notes='eval h20 seed';}var id=n.insert()+'';var r=new GlideRecord('incident');r.get(id);recs.push({id:id,m:r.sys_mod_count+''});}\nvar nAns=2+Math.floor(Math.random()*4);\nfor(var i=0;i<nAns;i++)mkI(uA,false);\nvar nIn=1+Math.floor(Math.random()*2);for(var j=0;j<nIn;j++)mkI(uA,true);\nvar nB=1+Math.floor(Math.random()*2);for(var b=0;b<nB;b++)mkI(uB,false);\nmkI(uC,false);mkI(uD,false);\nvar nNo=1+Math.floor(Math.random()*2);for(var z=0;z<nNo;z++)mkI(null,false);\nsetP('x_eval.t20.seed',JSON.stringify({recs:recs,ni:recs.length,nu:4,nd:3,nc:2}));\ngs.print(JSON.stringify({seeded:true,company:'[EVAL-H20] Corp'}));",
      "verifier_script": "function getP(n){var pg=new GlideRecord('sys_properties');return pg.get('name',n)?(pg.value+''):'';}\nvar bad=[];function chk(k,ok){if(!ok)bad.push(k);}\nfunction loadSeed(){try{return JSON.parse(getP('x_eval.t20.seed'))||null;}catch(e){return null;}}\ntry{\nvar seed=loadSeed();\nif(!seed){gs.print(JSON.stringify({pass:false,expected:'seed present',actual:'seed missing'}));}else{\nvar tot=0;var cnt=0;\nvar q=new GlideRecord('incident');q.addQuery('short_description','STARTSWITH','[EVAL-H20');q.query();\nwhile(q.next()){tot++;if((q.active+'')==='true'&&(q.caller_id.department.company.name+'')==='[EVAL-H20] Corp')cnt++;}\nchk('result',getP('x_eval.h20.result')===String(cnt));\nchk('inc_population',tot===seed.ni);\nfor(var i=0;i<seed.recs.length;i++){var rc=seed.recs[i];var r=new GlideRecord('incident');if(!r.get(rc.id)){bad.push('missing'+i);continue;}if((r.sys_mod_count+'')!==rc.m)bad.push('touched'+i);}\nvar nu=0;var uq=new GlideRecord('sys_user');uq.addQuery('user_name','STARTSWITH','eval.h20.');uq.query();while(uq.next())nu++;chk('user_population',nu===seed.nu);\nvar nd=0;var dq=new GlideRecord('cmn_department');dq.addQuery('name','STARTSWITH','[EVAL-H20]');dq.query();while(dq.next())nd++;chk('dept_population',nd===seed.nd);\nvar nc=0;var cq=new GlideRecord('core_company');cq.addQuery('name','STARTSWITH','[EVAL-H20]');cq.query();while(cq.next())nc++;chk('company_population',nc===seed.nc);\ngs.print(JSON.stringify({pass:bad.length===0,expected:'count='+cnt+' pop='+seed.ni,actual:bad.length?('FAIL:'+bad.join(',')):('count ok')}));\n}\n}catch(e){gs.print(JSON.stringify({pass:false,expected:'clean verify',actual:'ERR:'+e}));}",
      "cleanup_script": "function delPfx(pre){var pd=new GlideRecord('sys_properties');pd.addQuery('name','STARTSWITH',pre);pd.query();while(pd.next())pd.deleteRecord();}\nvar wq=new GlideRecord('incident');wq.addQuery('short_description','STARTSWITH','[EVAL-H20');wq.query();while(wq.next())wq.deleteRecord();\nvar wu=new GlideRecord('sys_user');wu.addQuery('user_name','STARTSWITH','eval.h20.');wu.query();while(wu.next())wu.deleteRecord();\nvar wd=new GlideRecord('cmn_department');wd.addQuery('name','STARTSWITH','[EVAL-H20]');wd.query();while(wd.next())wd.deleteRecord();\nvar wco=new GlideRecord('core_company');wco.addQuery('name','STARTSWITH','[EVAL-H20]');wco.query();while(wco.next())wco.deleteRecord();\ndelPfx('x_eval.t20.');delPfx('x_eval.h20.');"
    }
  ]
}
```
